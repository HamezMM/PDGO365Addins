<?xml version="1.0" encoding="utf-8"?>
<packages>
  <!-- Office interop and VSTO assemblies come from the GAC via the Office build
       targets, not NuGet. Add package entries here only for extra libraries. -->
</packages>
